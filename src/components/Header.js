import React from 'react'

const Header = (props) => {
  return (
    <header className={`${props.classes} h-16 p-4 bg-red-600 rounded-lg my-4 text-white shadow-lg`}>
      <h1 className="text-2xl font-bold">Contender Bike Sizer</h1>

      
    </header>
  )
}

export default Header
